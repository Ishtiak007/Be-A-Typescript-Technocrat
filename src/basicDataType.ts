// -------------------BASIC DATA TYPE---------------------

// string
let firstName: string = "Ishtiak";

// number
let roll: number = 107;

// boolean
let isAdmin: boolean = true;

// undefined
let x: undefined = undefined;

// null
let y: null = null;

// any type
let d: number;
d = 107;

// Array
let frinds: string[] = ["Ishtiak", "Imtiaz", "Monica", "Rechel"];
frinds.push("Ahmed");

let eligibleRollList: number[] = [1, 0, 7];

// Tuple -----> array ----> order ----> type of values
let coordinates: [number, number] = [1, 7];

let ageName: [number, string, boolean] = [107, "Ishtiak", true];
ageName[0] = 32;
